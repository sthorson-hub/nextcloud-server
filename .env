# domain + traefik bits
NEXTCLOUD_HOST=nextcloud.nexusfed.dev
TRAEFIK_ENTRYPOINT=websecure
TRAEFIK_CERTRESOLVER=le

# nextcloud admin (first login)
NC_ADMIN_USER=nextcloud120
NC_ADMIN_PASSWORD=uyghur32U

# database
MYSQL_DATABASE=nextcloud
MYSQL_USER=nextcloud
MYSQL_PASSWORD=uyghur32U
MYSQL_ROOT_PASSWORD=uyghur32U


# # DNS should point to the new tunnel
# nslookup nextcloud.nexusfed.dev

# # cloudflared -> traefik inside the proxy network
# docker exec -it cloudflared sh -lc 'apk add --no-cache curl >/dev/null; curl -sI -H "Host: nextcloud.nexusfed.dev" http://traefik:80/ | head -n5'

# # traefik -> nextcloud
# docker exec -it traefik sh -lc 'apk add --no-cache curl >/dev/null; curl -sI http://nextcloud-app:80/ | head -n1'
